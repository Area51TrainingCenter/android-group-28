<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="area51.clase02">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Clase02Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Clase02.Base">
        <activity
            android:name=".screen.welcome.view.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".screen.launcher.view.LauncherActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Clase02.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.product.view.ProductActivity" />
        <activity android:name=".screen.product.view.PhotoActivity" />
        <activity android:name=".screen.product.view.PhotoGridActivity"></activity>
    </application>

</manifest>